[workspace]
members = ["src-tauri", "src-wasm"]
resolver = "2"

# Profils valables pour tout le workspace (évitent les warnings)
[profile.dev]
incremental = true

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "z"
strip = true
